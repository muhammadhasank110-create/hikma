# HIKMA Accessibility Audit Log

## Session summary (2026-08-08 — Full Debug Pass)

Full debug sweep of all pages. 7 confirmed bugs found and fixed. TypeScript: 0 errors. Tests: 3/3 pass.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| B-001 | Navigation | SEV-HIGH | All / EN + AR | AppShell.tsx:271 | "More" dropdown does nothing on click/touch | CSS `group-hover` only — no click handler, broken on touch devices | Replaced with React `useState` toggle; `aria-expanded`, `aria-haspopup`, `role="menu"` added | Code review + TypeScript | ✅ Done |
| B-002 | Logo | SEV-HIGH | All | AppShell.tsx:234 | Logo renders as near-invisible faint white pixels | Used `hikma-falcon-white.png` (white pixels on transparent, 7KB) which is invisible at small sizes | Switched to `hikma-icon-dark.png` (dark green rounded square + white falcon, clearly visible on dark nav) + CSS text wordmark | Screenshot verified | ✅ Done |
| B-003 | Logo | SEV-MED | All | Home.tsx:303 | Home page nav logo uses composite PNG that renders poorly | `hikma-nav-white.png` is a programmatically composited image with rough quality | Replaced with same icon+text approach as AppShell for consistency | Code review | ✅ Done |
| B-004 | Typography | SEV-LOW | All | Dashboard.tsx:110, index.css:274 | Stat card numbers show as slashed zeros (ⓞ) | Atkinson Hyperlegible font has slashed zeros enabled by default | Added `font-feature-settings: "zero" 0` to body CSS; also `fontVariantNumeric: "normal"` on stat card numbers | Code review | ✅ Done |
| B-005 | Icon visibility | SEV-MED | Light/Cream themes | SubjectPage.tsx:43, TopicsPage.tsx:97 | Subject and topic icons are very faint in light mode | `bg-primary/10` container too transparent; `strokeWidth` default too thin | Increased to `bg-primary/15`, added `strokeWidth: 2.5` on BookOpen; topic number badge to `bg-primary/20` | Code review | ✅ Done |
| B-006 | Feedback | Enhancement | All / EN + AR | client/src/components/FeedbackPanel.tsx | No way for users to report bugs | Feature missing | Added FeedbackPanel to Settings page with screenshot upload and owner email notification | TypeScript + screenshot | ✅ Done |
| B-007 | Security | SEV-SECURITY | All | client/src/hooks/useTTS.ts | ElevenLabs API key exposed in client bundle | `VITE_ELEVENLABS_API_KEY` sent as `xi-api-key` header directly to api.elevenlabs.io | Removed client key; all TTS now routes through server proxy `/api/tts/speak` | grep acceptance check | ✅ Done |

## Matrix checks

| Check | Result |
|---|---|
| Keyboard-only | More dropdown: aria-expanded/haspopup/role=menu added; Escape closes via onBlur |
| Semantics | More dropdown: role="menu", role="menuitem" on links |
| Blind/Low-Vision | Logo alt="" aria-hidden="true" on decorative images |
| Dyslexia | font-feature-settings: "zero" 0 disables slashed zeros globally |
| ADHD/Focus | No changes to focus mode |
| Bilingual/RTL | More dropdown uses `start-0` (logical) not `left-0` |
| Voice parity | No new primary actions added |
| Device parity | More dropdown works on touch (click-toggled, not hover-only) |
| No regressions | TypeScript: 0 errors; Tests: 3/3 pass |

## Open items

- ElevenLabs 402 errors in console log from old cached requests (old key used premium voices) — these are stale log entries, not current failures. New key uses free-tier Bella voice.
- Slashed zeros fix requires browser reload to take effect (CSS change applied, HMR propagated).

## Edge-Case Debug Pass (2026-08-11)

| ID | Category | Symptom | Root cause | Fix | Status |
|---|---|---|---|---|---|
| E-001 | Search | Clicking **Search** did nothing | TopNav and CommandPalette owned different `cmdOpen` state values | Lifted the command-palette state to AppShell and passed a controlled open handler to both | ✅ Done |
| E-002 | Audio lifecycle | Narration carried on after route changes | Lesson-local TTS instances were not included in the shared navigation stop path | Added an app-wide stop event, route-change dispatch, and `useTTS` unmount cleanup | ✅ Done |
| E-003 | Navigation menu | **More** scrolled inside the nav instead of opening a menu | Menu content was positioned inside an overflow-scrolling nav container | Replaced it with a Radix dropdown portal, which anchors below the trigger and escapes the nav overflow | ✅ Done |
| E-004 | Lesson narration | Word highlighting could outlive Listen playback | Highlight state was only partly cleared on TTS completion | Clear highlight state on stop, section changes, and lesson unmount | ✅ Done |
| E-005 | Concept map | Diagram nodes overlapped and ignored active theme tokens | Radial layout used invalid SVG color variables and variable node spacing | Rebuilt as a bounded 1–3/2-column SVG layout with semantic colors, unique arrow IDs, and a robust list fallback | ✅ Done |
| E-006 | Settings | Selected/open controls were visually inconsistent | Settings depended on generic transparent Select surfaces; the nav settings control had invalid nested interaction | Added explicit semantic Select states, high-contrast selected goal state, and valid `Button asChild` navigation | ✅ Done |
| E-007 | Simplify | Literal `*` / `**` artifacts appeared while streaming | Markdown delimiters arrived in partial server-sent chunks | Normalise incomplete emphasis and list markers before rendering through Streamdown; added regression tests | ✅ Done |
| E-008 | Hikma AI | Chat content appeared to disappear after focus/contrast changes | Focus CSS globally recolored every paragraph and list item, including chat bubbles | Scoped focus typography to lesson content only; browser-session conversation persistence added as a safety net | ✅ Done |

### Verification

- `npx tsc --noEmit`: passed.
- `pnpm test`: 3 files / 5 tests passed, including new simplified-Markdown regression tests.
- Visual checks: Settings, Hikma AI, Dashboard, and Lesson route surfaces captured in the live development preview.

### Current operational note

The configured ElevenLabs key currently reports `quota_exceeded` with one credit remaining. The new narration lifecycle fixes are independent of that quota condition; until the key is topped up or replaced, speech uses the browser fallback after the server rejects an ElevenLabs request.

## Full Reliability Audit (2026-08-11)

| ID | Severity | Area | Confirmed root cause | Reliability repair | Verification |
|---|---|---|---|---|---|
| R-001 | P0 | Lesson narration | A cleanup effect depended on an unstable TTS object and repeatedly called state-changing stop logic. | Replaced the dependency with stable TTS methods and retained explicit route/unmount cancellation. | Lesson route renders without the React update loop; TypeScript passed. |
| R-002 | P1 | Navigation | Desktop, More, mobile, and command-palette paths did not share one filtered navigation source; Search was absent from mobile. | Centralized filtered navigation and exposed Search through the mobile path. | Code-path audit and responsive preview. |
| R-003 | P1 | Direct routes | Direct URL access had no explicit authentication/role boundary. | Added protected and role-aware route boundaries for learner, teacher, guardian, and admin surfaces. | Route tree and auth endpoint checked. |
| R-004 | P1 | Landing accessibility | Anonymous users lacked the minimum language, contrast, and text-size controls. | Added a compact pre-auth control cluster, retaining a mobile text-size increase action. | Mobile landing screenshot. |
| R-005 | P1 | Lesson state | Local Focus state could diverge from profile preference; completion navigation could fire after leaving a lesson; normal Markdown words could not be clicked for definitions. | Synchronized Focus with profile state, cleared completion timers, and added DOM caret-based English/Arabic word selection. | TypeScript passed. |
| R-006 | P1 | Assessment | Quiz generation could commit stale results after lesson/locale changes; recording used unused MediaRecorder buffers. | Added cancellation guards, actionable failed-load state, and direct SpeechRecognition cleanup. | TypeScript and test suite passed. |
| R-007 | P1 | Tutor | Streams and voice-input resources could survive page exit. | Abort tutor streams, release recognition, and stop local narration on unmount. | TypeScript passed. |
| R-008 | P1 | Global speech | Playback-state rerenders recreated shared speech callbacks, allowing accessibility effects to cancel narration unexpectedly. | Memoized stable shared speech methods and context value; narrowed focus-profile effect dependencies. | TypeScript and regression suite passed. |
| R-009 | P1 | Dark theme | The profile stored `data-theme="dark"` but did not set Tailwind’s `.dark` class, so dark variants failed. | Synchronize `.dark` with the profile theme. | Mobile dashboard visual check. |
| R-010 | P2 | TTS quota | Exhausted ElevenLabs quota caused repeated failed requests. | Detect quota/auth failure once per browser session and switch cleanly to browser speech. | Static review; external quota remains an operational limit. |

### Reliability verification matrix

| Verification | Result |
|---|---|
| TypeScript | `npx tsc --noEmit` passed. |
| Automated tests | `pnpm test` passed: 3 files, 5 tests. |
| Server auth response | `/api/trpc/auth.me` returned in 52 ms for the unauthenticated case. |
| Mobile visual checks | Landing, dashboard, lesson, quiz, tutor, and settings routes captured at 375×812. |
| Dark/light token behavior | Dashboard visual check confirmed readable counters, icons, and cards after theme-class synchronization. |
| Known external limit | ElevenLabs remains quota-exhausted until its account is topped up or a fresh key is supplied; session fallback prevents repeated failed requests. |

## Motion, SEO, and render-stability session (2026-08-12)

This session introduced a shared, profile-aware Motion vocabulary for page transitions, staged lists, dashboard action feedback, progress indicators, navigation surfaces, onboarding, and pending-content skeletons. Landing-page decorative canvas and mouse-follow effects were removed from the rendered tree, while the remaining non-essential background and headline motion now respects learner reduced-motion preferences and background-tab visibility. The public route was also given a compliant title, keywords, semantic primary heading, and image alternatives. A reported provider-tree render failure was traced to a `localStorage` write inside `useAuth` render-time memoization; storage persistence now runs in a guarded effect so restricted-storage contexts cannot interrupt the authentication provider.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| MOT-01 | Motion accessibility | P2 | ADHD/Focus and system reduced-motion users; English and Arabic layouts | `client/src/lib/motion.ts`, `client/src/components/PageTransition.tsx`, `client/src/components/AppShell.tsx`, `client/src/pages/Dashboard.tsx`, `client/src/pages/Onboarding.tsx`, `client/src/pages/Home.tsx` | Repeated UI motion used independent timing and did not consistently honor learner profile preferences. | Motion behavior was defined locally in page components rather than through a shared profile-aware configuration. | Added typed motion tokens and a `useHikmaMotion` hook; updated reusable transitions, progress, shell overlays, dashboard cards, loading states, onboarding, and priority landing motion to use short transform/opacity-only patterns or static fallbacks. | `npx tsc --noEmit`; Vitest suite; desktop landing capture and mobile captures of landing and dashboard. Arabic/RTL interaction was not re-run in this session. | Done |
| SEO-01 | Public landing semantics | P3 | Public route `/`; English and Arabic alternative text | `client/index.html`, `client/src/pages/Home.tsx` | The page lacked a keywords meta tag and a rendered H1; its title was too short and landing images lacked descriptive alternatives. | Metadata and primary heading were not specified in the original landing implementation. | Added six focused keywords, a 48-character document title, a single descriptive H1, and non-empty logo/watermark alternatives. | Automated field check confirmed 6 keywords, title length 48, and 1 H1; visual landing capture completed. | Done |
| R-011 | Render stability | P1 | All profiles/locales in browser contexts where storage is unavailable or restricted | `client/src/_core/hooks/useAuth.ts` | The provider tree could fail during rendering with a stack trace through `useAuth/state`. | `localStorage.setItem` executed inside `useMemo` during render and was not guarded against storage security failures. | Moved storage synchronization into `useEffect` and contained storage exceptions; tRPC remains the authentication source of truth. | Public landing route renders after a full hot reload; `npx tsc --noEmit` and all 7 Vitest checks passed before the final shell and onboarding motion adjustments. | Done |
| MOT-02 | Visual contrast | P2 | Dark and high-contrast profile surfaces | `client/src/pages/Dashboard.tsx`, `client/src/index.css` | Gradient-card content became too low-contrast in the active dark profile. | Gradient surfaces and palette-specific utility classes did not track the semantic profile tokens. | Replaced gradient card backgrounds with semantic card and accent surfaces that inherit the active profile’s foreground tokens. | Desktop dashboard screenshot confirmed the dark treatment; a token audit confirmed high contrast resolves `--card` to black and `--card-foreground` to white. | Done |

## Product redesign and reliability session (2026-08-12)

The public route now begins with a minimal animated brand entry before revealing a quieter learning introduction. Particle canvas rendering, animated orbs, mouse-follow effects, marquee motion, and glow-heavy calls to action were removed. Authenticated learner routes use a simplified section-aware top navigation and a five-item bottom navigation on narrow screens. Settings now holds quick language and access controls instead of the public top utility strip. Sound playback is guarded against restricted storage and unavailable browser audio, the analytics include was removed, and tutor session identity is retained within the browser session while the server prompt receives only factual stored-progress counts.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| UI-01 | Visual hierarchy and motion | P3 | Public route; system reduced-motion learners | `client/src/pages/Home.tsx` | The landing route was visually crowded with particle, orb, cursor, marquee, and glow effects. | Decorative motion dominated the product purpose and consumed rendering work without supporting a learner task. | Replaced the route with a branded entry overlay, concise responsive hero, and minimal feature cards; retained profile-aware Motion only for entry and content continuity. | Desktop and 375×812 landing captures; TypeScript and unit suite passed. Direct reduced-motion interaction was not re-run. | Done |
| NAV-03 | Responsive navigation | P2 | Learner mobile routes; English UI | `client/src/components/AppShell.tsx`, `client/src/pages/Dashboard.tsx` | Learner routes did not offer phone-app-style navigation or an obvious compact next-learning action, and keyboard search had no global trigger. | Navigation access was concentrated in the top shell and the learner-home hierarchy did not prioritize continuation on mobile. | Added role-filtered bottom learner navigation at mobile widths, a primary Continue learning action, and Ctrl/Cmd+K activation for the existing animated command search. | 375×812 dashboard and settings captures showed the fixed bottom navigation and learner-home action. Keyboard activation and Arabic/RTL parity remain manual follow-ups. | Done |
| REL-01 | Runtime reliability | P1 | Restricted-storage browsers and sound-enabled learners | `client/src/lib/sound.ts`, `client/index.html`, `client/src/pages/LessonPage.tsx` | A click could throw through `playSound`; lesson narration needed an explicit stop action; an external analytics include generated console failures. | Sound read preferences directly from browser storage and assumed Web Audio; analytics loaded a failing external endpoint. | Replaced the sound helper with guarded storage and AudioContext access, wired the active lesson audio control to stop, and removed the analytics include. | TypeScript and 5 Vitest files / 8 checks passed, including the new storage-safety regression test; the latest 80 console lines contained no matching `playSound`, analytics, umami, SecurityError, or Error entries. | Done |
| TUT-01 | Tutor personalization | P2 | Authenticated tutor users | `client/src/hooks/useTutorState.ts`, `server/routers/tutor.ts` | Tutor identity could reset across safe remounts and prompts lacked factual progress summary. | Session ID was generated per mount and stored learning counts were not included in tutor context. | Persisted a browser-session tutor ID and added completed-lesson/mastery-record counts to the server prompt, with an explicit prohibition on inventing gaps or scores. | TypeScript and unit suite passed; authenticated tutor interaction remains a manual follow-up. | Done |
| HYG-03 | Code hygiene | P3 | All users | `client/src/pages/ComponentShowcase.tsx` | An unreferenced component-library demo remained in product source. | Legacy scaffold artifact was not routed or imported. | Removed after a repository reference search. | Repository-wide reference search showed no matches and TypeScript passed after deletion. | Done |

## Grounded tutor and localization session (2026-08-12)

The public brand entry now advances automatically after a brief, reduced-motion-safe interval. The command dialog’s title and description now reside within its content, resolving the Radix accessibility warnings associated with the attached passive-effect stack. The landing page provides Arabic copy, RTL direction, and an Arabic-optimized type stack; the existing shared profile direction remains the source of truth for authenticated routes. The tutor can retrieve up to three current scholarly metadata records from OpenAlex for study-skills questions, passes them to the model as untrusted reference data, and renders the returned citations with bilingual explanatory labels. Source retrieval fails closed: if the external service is unavailable, no current-source claim is added.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| A11Y-12 | Dialog semantics | P2 | Keyboard and screen-reader users | `client/src/components/ui/command.tsx` | Dialog-title and description warnings appeared through a passive effect. | The hidden heading was a sibling of `DialogContent`, so Radix could not associate it with the dialog surface. | Moved the hidden title and description inside `DialogContent`; also corrected an invalid utility selector. | TypeScript passed; Playwright browser suite passed across desktop and 375×812 viewport projects. | Done |
| UI-04 | Automatic entry | P3 | Public route; reduced-motion learners | `client/src/pages/Home.tsx` | The new brand entry required a manual button press. | Entry state had no timed progression. | Added a cancellable automatic transition: 1.6 seconds by default and 80 ms when reduced motion is enabled. | Playwright verifies entry disappearance and landing H1 on desktop and narrow viewport. | Done |
| RTL-04 | Arabic presentation | P2 | Arabic/RTL public landing and shared RTL surfaces | `client/src/pages/Home.tsx`, `client/src/index.css` | The redesigned public surface retained English-only content and Latin display spacing. | Landing copy was static; RTL typography had no dedicated global override. | Added Arabic landing strings, direction-aware content and icon direction, plus an Arabic-optimized font fallback and RTL spacing rules. | TypeScript passed. Authenticated Arabic route interaction remains a manual follow-up because the browser test suite has no authenticated learner fixture. | Done |
| TUT-02 | Grounded current-study guidance | P1 | Tutor study-skills queries; English and Arabic citation labels | `server/studyGrounding.ts`, `server/tutorStream.ts`, `client/src/hooks/useTutorState.ts`, `client/src/pages/TutorPage.tsx` | Tutor answers could not distinguish current external evidence from generated guidance. | No retrieval or citation channel was present in the tutor stream. | Added bounded OpenAlex metadata retrieval, untrusted-source prompt isolation, failure-safe fallback, SSE source delivery, and accessible source citations in the tutor UI. | 3 retrieval unit tests passed for gating, mapping, and failure fallback; full suite: 6 files / 11 tests. Live authenticated source display remains a manual follow-up. | Done |
| TEST-04 | Browser regression coverage | P2 | Public desktop and narrow mobile layouts | `playwright.config.ts`, `e2e/public-and-tutor.spec.ts` | Critical entry and protected-tutor boundaries lacked browser coverage. | Project had no browser test runner. | Added serial Chromium Playwright configuration and coverage for automatic entry, landing H1 semantics, protected tutor route behavior, and landing heading fit at 375×812. | `pnpm test:e2e`: 7 passed, 1 project-specific assertion skipped. Authenticated tutor conversation, source-citation rendering, search interaction, and Arabic profile switching require an authenticated test fixture. | Done |
| SEC-04 | Role provisioning | P1 | Administrator and teacher roles | `ACCESS_PROVISIONING.md`, `drizzle/schema.ts`, `server/db.ts` | The requested predictable administrator/teacher credentials would expose privileged access. | Authentication uses provider identifiers and roles rather than a secure seeded username/password bootstrap. | Documented secure first-sign-in and audited role-assignment flow for existing `admin` and `teacher` roles; no credentials were added. | Role enum and owner promotion path reviewed. | Done |

## Public pages and interaction polish session (2026-08-12)

HIKMA now has separate About and Contact pages with responsive layouts, restrained perspective depth, bilingual surface text, semantic form labels, and public navigation. The approved Contact fallback opens the visitor’s configured email client with the recipient, subject, and message populated; no mail-provider credentials are stored or exposed. The shared theme provider now begins from the device preference when no explicit choice is active. Keyboard users receive a tested main-content skip link, and the tutor header provides a live voice indicator for idle, listening, and speaking states that degrades to a static indicator when motion is reduced. Public SEO now includes canonical, robots, social, structured-data, manifest, robots, and sitemap signals.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| PUB-01 | Public information architecture | P3 | English and Arabic public routes; desktop and narrow viewport | `client/src/pages/AboutPage.tsx`, `client/src/pages/ContactPage.tsx`, `client/src/App.tsx`, `client/src/pages/Home.tsx` | Public visitors had no dedicated explanation of HIKMA or structured contact route. | The public route set consisted of the landing and authentication pages only. | Added responsive About and Contact routes, top navigation links, bilingual labels, and semantic name/email/subject/message fields. | Playwright About and Contact route checks passed on desktop and narrow viewport projects. | Done |
| PUB-02 | Contact fallback | P3 | Public Contact page | `client/src/pages/ContactPage.tsx`, `server/contactEmail.ts` | Automated server delivery requires a sender credential, which was not approved. | Secure email providers need an API key and verified sender; none is available. | Retained the approved `mailto:` fallback and built a no-secret server configuration guard instead of inventing credentials. | Unit guard passed; browser test confirms recipient mail link. Actual client-email dispatch remains dependent on the visitor’s configured mail application. | Done |
| A11Y-13 | Keyboard and voice feedback | P2 | Keyboard users; Blind/Low-Vision learners; reduced-motion users | `client/index.html`, `client/src/components/VoiceActivityIndicator.tsx`, `client/src/pages/TutorPage.tsx`, `client/src/pages/ContactPage.tsx` | Skip targets were inconsistent, speech had no compact shared activity feedback, and Contact feedback did not return focus. | Public main landmarks lacked IDs; speech state was exposed only through individual controls; contact fallback had no focus target. | Added main-content landmarks, tested skip link, live voice activity labels/waveform with a static reduced-motion fallback, and focus restoration to the bilingual Contact status message. | Playwright skip-link and contact status-focus checks passed; voice-state label unit tests passed. Authenticated live voice state requires an authenticated browser fixture. | Done |
| UX-07 | Theme and motion | P3 | Device light/dark preference; explicit accessibility profile themes; reduced-motion users | `client/src/contexts/ThemeContext.tsx`, `client/src/components/PageTransition.tsx`, `client/src/index.css` | Initial theme did not adopt device preference and transition depth lacked a shared refinement. | Theme provider defaulted to light; no shared perspective layer existed. | Added device-theme observer with guarded storage access, protects non-light profile themes, and adds a subtle shared depth transition; reduced-motion returns static content. | Playwright dark-theme test and TypeScript passed; non-light profile preservation is enforced in the theme provider. | Done |
| SEO-02 | Public discoverability | P3 | Public routes | `client/index.html`, `client/src/hooks/usePageMetadata.ts`, `client/public/robots.txt`, `client/public/sitemap.xml` | Public routes shared landing metadata and lacked crawler assets. | Metadata was static and no route helper or crawl assets existed. | Added per-route metadata updates, canonical URLs, robots directives, structured organization data, robots.txt, and sitemap.xml. | Playwright checks robots and canonical fields; route-level title helper type-checked. | Done |

## Tutor wave and reusable workflow session (2026-08-12)

Tutor playback now presents a compact twelve-bar waveform while narration is active, with staggered amplitudes, a screen-reader status, a visible Stop narration control, and a static reduced-motion fallback. Public routes can be previewed in Arabic through `?lang=ar`, allowing direction and localization coverage without a signed-in learner. Browser tests use an in-memory intercepted learner response and mocked browser speech APIs rather than real credentials. The reusable `hikma-experience-polish` skill was initialized with the skill-creator workflow and passed its validator.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| TUT-03 | Tutor voice feedback | P2 | Narration users; reduced-motion users | `client/src/components/VoiceActivityIndicator.tsx`, `client/src/pages/TutorPage.tsx`, `client/src/index.css` | Active narration had only a compact generic indicator and no direct stop action in the header. | Playback state was represented as four uniform bars without action-level cancellation affordance. | Added a twelve-bar dynamic speaking waveform with staggered amplitude, preserved concise listening and static reduced-motion modes, and added a labelled Stop narration control. | Voice-label unit tests and credential-free authenticated Playwright narration start/speaking/stop fixture passed. | Done |
| RTL-05 | Arabic public navigation | P2 | Public Arabic routes and narrow viewport | `client/src/contexts/ProfileContext.tsx`, `client/src/pages/Home.tsx`, `e2e/public-and-tutor.spec.ts` | Arabic public navigation could not be regression-tested without a database-backed profile, and the entry copy remained English-only. | The public locale always started in English and the branded entry had static English strings. | Added a public `?lang=ar` initialization mode and localized the brand entry; existing profile direction synchronization sets document language and direction. | Playwright asserts Arabic `lang`, RTL direction, labels, automatic entry completion, and stable Arabic navigation into the About route. | Done |
| TEST-05 | Authenticated tutor browser fixture | P2 | Tutor voice controls | `e2e/tutor-voice-authenticated.spec.ts` | Tutor narration could not be browser-tested without a privileged seeded account. | Tests depended on the real authentication response and browser speech implementation. | Intercepts the `auth.me` tRPC response with a safe in-memory learner and mocks browser speech and TTS configuration. | Desktop and narrow viewport tutor voice flow passed without embedded credentials. | Done |
| SKILL-01 | Reusable procedure | P3 | Future HIKMA maintenance work | `/home/ubuntu/skills/hikma-experience-polish/SKILL.md` | The validated accessibility and browser-testing process was not reusable. | Workflow knowledge existed only in this implementation session. | Created a concise skill covering motion, voice, Arabic/RTL, theme, SEO, keyboard, and credential-free Playwright patterns. | `quick_validate.py hikma-experience-polish` completed successfully. | Done |

## Narration-synchronized lesson highlighting session (2026-08-13)

Lesson narration now follows the spoken word in both the normal reading surface and Focus mode. The DOM-based renderer rebuilds its text-node word map after each prior mark is removed, preventing stale offsets as narration progresses. Standard reading uses a restrained primary-color underline wash, while Focus mode uses a clearer green treatment. Neither style adds layout-affecting spacing. High-contrast mode inverts the active word to yellow on black, inactive speech removes the mark entirely, and reduced-motion learners retain the active word without smooth scroll transitions.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| LES-01 | Narration tracking | P2 | Standard reading, Focus mode, Arabic/RTL, high-contrast, and reduced-motion learners | `client/src/pages/LessonPage.tsx`, `client/src/lib/narrationHighlight.ts`, `client/src/index.css` | Narration highlighting could lose alignment after a word mark split a text node, and the visual treatment did not clearly distinguish focus reading. | The DOM word list was built before mark insertion and reused after DOM mutation. | Rebuild the word map after mark removal; centralize active/focus/reduced-motion state; render semantic mark styles with no layout shift. | TypeScript passed; 9 Vitest files / 17 tests passed, including three narration-state tests; Playwright suite: 23 passed, 1 intentionally skipped. | Done |

## Exact listening-word synchronization correction (2026-08-13)

The narration lifecycle now keeps the browser TTS offset map synchronously available before the first boundary is emitted. Each speech boundary is translated through a tested binary lookup into the exact spoken word index. The lesson view tags only that word with `data-current-spoken-word`; no mark is present when listening is inactive. Focus mode changes the treatment of the same exact word rather than starting a second narration state. The lesson Stop control now runs one shared cleanup path that clears state and removes any mark from the rendered text immediately.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| LES-02 | Exact spoken-word highlight | P1 | Standard lesson listening, Focus listening, desktop and mobile | `client/src/hooks/useLessonState.ts`, `client/src/pages/LessonPage.tsx`, `client/src/lib/narrationHighlight.ts`, `e2e/lesson-narration-highlight.spec.ts` | Users could not reliably see the exact word being read, and a stale mark could remain after stopping narration. | Initial browser speech boundaries can precede React state-ref synchronization; stopping depended on a later render effect. | Synchronize offsets into a ref before calling TTS, use `findSpokenWordIndex` for boundary lookup, mark only the current spoken word, and invoke deterministic DOM/state cleanup from the Stop control. | 9 Vitest files / 18 tests passed, including exact boundary mapping; Playwright drives boundaries `0`, `6`, and `11` to verify the active mark advances `Alpha → beta → gamma.`, remains unique, adopts Focus styling, and is removed on stop across desktop and 375×812 projects. Full browser suite: 25 passed, 1 skipped. | Done |

## Browser speech-boundary resilience session (2026-08-13)

The initial mitigation for browser engines that omit later word boundaries used a guarded elapsed-time fallback. Subsequent live feedback showed that elapsed estimates could not remain synchronized with audible narration, so this mitigation was **superseded** by the audio-position synchronization correction below. The current browser path advances only from native word-boundary events, and the current streamed path uses provider timestamps matched to the audio element clock.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| LES-03 | Browser TTS resilience | P1 | Browser speech on desktop and mobile; standard and Focus modes | `client/src/hooks/useTTS.ts`, `e2e/lesson-narration-highlight.spec.ts` | Some browser engines omit later `onboundary` events. | Browser TTS does not expose an audio clock that can safely replace missing boundaries. | Initial elapsed-time fallback was removed because it drifted; current browser behavior follows native boundaries only and timestamp-capable streamed narration provides exact audio synchronization. | The original boundary-only fixture is retained as historical coverage; actual-audio synchronization is verified under LES-05. | Superseded |
| LES-04 | Streamed-audio narration tracking | P1 | Server-proxied streamed audio on desktop and mobile | `client/src/hooks/useTTS.ts`, `e2e/lesson-streamed-audio-highlight.spec.ts` | The browser-TTS remediation needed proof that it did not regress the existing request-animation-frame streamed-audio path. | Browser and streamed-audio playback use different timing engines. | Left the streamed path event model intact and added an isolated mocked Audio/RAF fixture. | The fixture progresses `Alpha → beta → gamma.` through real request-animation-frame timing and confirms a single active mark plus stop cleanup on desktop and 375×812. Full validation: 9 Vitest files / 18 tests; 27 Playwright passed, 1 skipped. | Done |

## Audio-position synchronization correction (2026-08-13)

Estimated highlight cadence and duration-ratio word selection were removed because neither represents the actual spoken position. Browser speech now advances only when the browser provides an actual word boundary; it never invents a later word based on elapsed wall-clock time. When the server-proxied provider is available, the app requests speech with provider character alignment, converts the exact character start timestamps to lesson word offsets, and selects the highlighted word from the HTML audio element’s actual `currentTime`. The capability probe now checks that timestamped synthesis is available, rather than merely checking ordinary audio generation. The official provider endpoint returns audio together with character-level start times for audio-text synchronization.[1]

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| LES-05 | Audio-highlight synchronization | P1 | Browser speech and timestamp-capable streamed narration; standard and Focus modes | `server/_core/elevenLabsTTS.ts`, `client/src/hooks/useTTS.ts`, `client/src/hooks/useTTS.test.ts`, `e2e/lesson-streamed-audio-highlight.spec.ts` | Highlighted words could run ahead of or behind audible narration because they were derived from elapsed estimates rather than audio alignment. | Browser fallback cadence and streamed duration-ratio selection did not use a speech-aligned source of truth. | Browser path uses native word boundaries only; streamed path uses ElevenLabs character alignment and `audio.currentTime` to choose the actual active word. Alignment failure falls back to browser boundaries rather than an estimate. | Unit tests map `Alpha beta gamma.` to aligned starts `0`, `.36`, `.66` and select the matching word by audio time; desktop and 375×812 streamed-audio fixtures verify `Alpha → beta → gamma.` at those real timestamps with stop cleanup. Full validation: 10 Vitest files / 20 tests; 27 Playwright passed, 1 skipped. | Done |

## Live browser narration movement correction (2026-08-13)

Live diagnostics showed that the deployed lesson route reports `hasElevenLabs: false`, so it does not receive timestamped provider audio and instead uses browser speech. In that environment, relying on `onboundary` alone allowed the visible marker to remain on the first word. Lesson narration now uses a **lesson-only segmented browser speech path**: each exact word is a separate speech utterance, and the mark changes in the corresponding utterance’s `onstart` callback. This means a new word cannot be highlighted until its audio segment begins. The generic tutor path keeps its original browser behavior; only lesson reading activates this precise fallback. The lesson container exposes `data-narration-sync` for live diagnostics and browser coverage: `browser-segmented`, `browser-boundary`, `timestamped-audio`, or `idle`.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| LES-06 | Live lesson highlight movement | P1 | Lesson browser-speech fallback, standard and Focus modes, desktop and 375×812 | `client/src/hooks/useTTS.ts`, `client/src/hooks/useLessonState.ts`, `client/src/pages/LessonPage.tsx`, `e2e/lesson-narration-highlight.spec.ts` | Live lesson highlights could remain on the first word because the active provider was browser speech with unavailable provider timestamps. | Server capability logs repeatedly returned `hasElevenLabs: false`; browser-boundary-only updates were not sufficient in that provider state. | Add lesson-only sequential word utterances, set the active word at each utterance’s real `onstart`, cancel with a run identifier, and expose the active sync source on the reading container. | Targeted tests verify `browser-segmented → idle` and `Alpha → beta → gamma.` in standard and Focus modes. The full browser suite was run twice consecutively: both runs passed 27 tests with 1 intentional skip; no narration errors appeared in the latest diagnostic log review. | Done |

### References

[1] [ElevenLabs — Create speech with timing](https://elevenlabs.io/docs/api-reference/text-to-speech/convert-with-timestamps)

## Premium responsive interface redesign (2026-08-13)

HIKMA’s presentation layer has been reworked into a more editorial, product-led experience inspired by the compositional discipline of the user-provided reference site, without reusing its branding or assets. The public experience now has a single shared layout, refined navigation, responsive product-preview storytelling, and low-contrast ambient depth. Authenticated routes retain the existing role, navigation, profile, and learner-data behavior while the dashboard and top shell now use a more deliberate surface hierarchy. Ambient animation is progressive enhancement only: it pauses in hidden tabs and is disabled for reduced-motion and low-capability device signals.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| UI-05 | Public visual hierarchy | P3 | Public English and Arabic routes; desktop and narrow viewport | `client/src/components/PublicLayout.tsx`, `client/src/components/AmbientBackdrop.tsx`, `client/src/pages/Home.tsx`, `client/src/pages/AboutPage.tsx`, `client/src/pages/ContactPage.tsx` | Public routes used duplicated headers and flat, route-specific compositions that did not communicate a coherent product system. | Public layout and visual treatment were implemented independently per page. | Added a shared public layout, premium editorial hierarchy, bilingual route-preserving navigation, low-contrast ambient depth, and responsive product-learning surfaces. | Desktop and 375×812 captures reviewed for Home, About, and Contact. Browser coverage passed for automatic entry, one H1, navigation, Contact form/fallback, SEO, and Arabic route stability. | Done |
| UI-06 | Authenticated app hierarchy | P3 | Learner desktop and mobile shell | `client/src/components/AppShell.tsx`, `client/src/pages/Dashboard.tsx` | Learner chrome and dashboard surfaces lacked a consistent product-level depth and task hierarchy. | Existing cards and navigation were functionally sound but visually fragmented. | Refined the sticky desktop navigation, selected navigation pills, premium dashboard continuation surface, elevated semantic cards, and mobile-safe dashboard spacing without changing role-filtered navigation behavior. | TypeScript passed; protected tutor and learner-path browser coverage passed. | Done |
| PERF-02 | Ambient motion governance | P2 | Reduced-motion users, hidden tabs, and low-capability devices | `client/src/components/AmbientBackdrop.tsx`, `client/src/hooks/useHikmaMotion.ts`, `client/src/index.css` | Non-essential ambient movement could consume rendering resources despite not conveying learner state. | Decorative depth lacked an explicit low-capability guard. | Ambient transforms use opacity/transform only, pause when hidden, and remain static for profile/OS reduced motion or low-memory/low-core devices. | TypeScript passed; existing device-theme/reduced-motion routes retained browser coverage. Recent browser-console review showed no matching error or warning entries. | Done |
| A11Y-14 | Premium profile inheritance | P2 | Light, dark, cream, calm, and high-contrast profiles | `client/src/index.css`, `client/src/components/PublicLayout.tsx`, `e2e/tutor-voice-authenticated.spec.ts` | New public visual styling risked bypassing semantic learner-profile tokens and retaining decorative background in high contrast. | Premium styles initially used page-specific colors without a shared profile-aware abstraction. | Added shared premium canvas/surface/ink/border tokens and a high-contrast rule that removes only the decorative ambient backdrop while preserving semantic elements. | TypeScript passed; credential-free authenticated dashboard fixture confirmed `--card` resolves to cream `255 253 248`, calm `248 250 248`, and high contrast `0 0 0`. | Done |
| RTL-06 | Public locale continuity | P2 | Arabic public navigation | `client/src/components/PublicLayout.tsx`, `e2e/public-and-tutor.spec.ts` | The Arabic public URL mode could reset after following a public navigation link. | Public links did not preserve `lang=ar` across route changes. | Shared public navigation appends the active Arabic locale parameter to internal public links and approach anchors. | Desktop and narrow browser tests confirm Arabic document direction, localized links, and Arabic route continuity. | Done |

### Reference

[2] [Amplemarket homepage](https://www.amplemarket.com/)

## Playback performance and interactive feedback session (2026-08-14)

The attached recording identified that the previous lesson-only word-segmented browser speech strategy made narration choppy and slow. That strategy has been removed. Lessons now use a single natural utterance so browser voices keep normal prosody and pace; highlights move from genuine `onboundary` events, while provider-aligned streamed audio retains its own timestamp path. The first Listen action keeps narration state active before browser `onstart`, waits one macrotask after cancellation before speaking, and resumes browser speech before queueing the utterance. These safeguards prevent the prepared highlight state from being cleared before the first real boundary. The stop affordance now uses a square stop icon, and active lesson narration receives the shared lightweight voice-wave feedback.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| REL-03 | First lesson narration | P1 | Browser-speech lesson fallback; standard and Focus modes | `client/src/hooks/useTTS.ts`, `client/src/hooks/useLessonState.ts` | First Listen could appear inactive or clear its prepared highlight before playback began. | Narration state was set only in browser `onstart`; an effect could clear lesson highlight state between `speak()` and the first event, and Chromium can ignore an utterance queued immediately after cancel. | Set narration state before queueing, defer queueing one macrotask after cancel, resume speech before speak, and invalidate cancelled runs. | Natural sentence fixture passes on desktop and 375×812, including first Listen, native boundary advance, Focus styling, and stop cleanup. | Done |
| PERF-03 | Choppy lesson audio | P1 | Browser speech lesson fallback | `client/src/hooks/useTTS.ts`, `client/src/hooks/useLessonState.ts` | Recording showed long pauses between every spoken word and correspondingly jerky highlights. | Lesson narration split one sentence into serial one-word utterances to compensate for unreliable boundaries. | Replaced word segmentation with one natural sentence utterance; highlights are driven only by native boundaries or provider timestamps, never an estimated cadence. | Attached recording diagnosis reviewed; browser fixture verifies real native boundaries at `0`, `6`, and `11`. | Done |
| UI-07 | Lively playback feedback | P3 | Learners using narration; reduced-motion users | `client/src/pages/LessonPage.tsx`, `client/src/components/VoiceActivityIndicator.tsx` | Lesson playback lacked a clear active-state signal and used a muted-speaker symbol for a stop action. | Playback feedback was limited to text and an ambiguous icon. | Added shared speaking waveform, clear word-tracking feedback, a square stop icon, and static reduced-motion behavior. | TypeScript and focused browser narration suite passed. | Done |
| PERF-04 | Ambient rendering cost | P2 | Public mobile routes and low-capability devices | `client/src/components/AmbientBackdrop.tsx` | Large animated blurred layers could contribute unnecessary paint work. | Three large `blur-3xl` elements animated at every viewport size. | Reduced blur and dimensions, hid secondary layers below tablet/desktop breakpoints, and retained existing hidden-tab, reduced-motion, and low-capability guards. | TypeScript passed; public desktop/mobile layout and browser suite passed. | Done |
| REL-04 | Partial Web Speech compatibility | P1 | Browser speech implementations without `resume()` | `client/src/hooks/useTTS.ts`, `e2e/lesson-narration-highlight.spec.ts` | A partial speech-synthesis implementation could throw before the first sentence was queued. | The first-playback repair called `speechSynthesis.resume()` unconditionally. | Guarded the optional method before speaking while retaining the post-cancel queue delay. | Focused narration fixture passed with the partial API path; full validation passed: 10 Vitest files / 20 tests and 31 Playwright passed, 1 skipped. | Done |

### References

[3] [Provided Figma site](https://tawny-rally-44666612.figma.site/)  
[4] [Provided Figma accessibility-review board](https://www.figma.com/design/il0cCyKYozMTjq9CF1Pz7l/Accessibility-Review-Template--Community-?node-id=0-1&p=f&t=oaDXHP4SfEHCWd9E-)

## ECC navigation key correction (2026-08-15)

The reported React warnings originated in the shared command palette, which included role-filtered `visibleItems` and then manually added the same `/ecc` and `/exam-skills` routes again. When the command dialog mounted on `/ecc/1`, the two duplicate `href` values became duplicate React keys. The command list now uses the role-filtered source items plus Settings and Shortcuts only, then defensively deduplicates by `href`. The investigation also found two `main-content` IDs, one in the shared shell and one in Dashboard; Dashboard is now an ordinary container so the shell remains the sole skip-link target.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| NAV-04 | React reconciliation | P2 | ECC and Exam Skills command entries; desktop and mobile | `client/src/components/AppShell.tsx` | React reported duplicate keys `/ecc` and `/exam-skills` while the command palette mounted. | The route list duplicated items already supplied by the visible navigation model. | Removed hard-coded duplicate routes and deduplicated command candidates by `href` before rendering. | Authenticated desktop and mobile browser fixture opens the palette, confirms one ECC and one Exam Skills entry, and captures no duplicate-key console error. | Done |
| A11Y-15 | Landmark identity | P2 | Dashboard keyboard and screen-reader users | `client/src/components/AppShell.tsx`, `client/src/pages/Dashboard.tsx` | The regression fixture surfaced two elements with `id="main-content"`. | Dashboard repeated the identifier owned by the shared application shell. | Converted the Dashboard root to a regular container, leaving one shared main skip target. | Authenticated browser fixture passed across desktop and mobile. | Done |
| ECC-01 | Reported route coverage | P2 | `/ecc/1`; desktop and mobile | `e2e/tutor-voice-authenticated.spec.ts` | The exact reported route did not have a direct rendering regression check. | Existing fixtures covered shared navigation but not the ECC-area queries. | Added credential-free fixtures for `ecc.areas`, `ecc.units`, and `ecc.myProgress`, then rendered `/ecc/1` while recording duplicate-key errors. | The route shows its area heading and unit tab with no duplicate-key console errors. Targeted suite: 8 passed. | Done |

## Core learning MVP completion session (2026-08-15)

HIKMA already had durable curricula, subjects, topics, lessons, sections, progress, mastery, attempts, tutor conversations, learner profiles, and accessibility preferences. This session completed the material learner-facing gaps without adding mock educational records. Onboarding now persists optional subject interests and learning methods; the database migration is additive and legacy null values normalize to empty arrays. Practice now provides progressive hints and retry rather than treating an initial mistake as a dead end. The learner Dashboard and Progress page now derive continuation, named mastery, and review recommendations from stored records. Search now discovers real subject, topic, and lesson records alongside fixed navigation.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| MVP-01 | Learner preferences | P2 | New and returning learners; English and Arabic onboarding/Settings | `drizzle/schema.ts`, `server/routers/profile.ts`, `client/src/pages/OnboardingSteps.tsx`, `client/src/pages/Onboarding.tsx`, `client/src/pages/SettingsPage.tsx` | Onboarding could not retain subject priorities or preferred learning approaches, and preferences could not be revised later. | Learner profile lacked durable fields and Settings had no matching controls. | Added additive TiDB-compatible JSON fields, legacy null normalization, and accessible bilingual controls in both onboarding and Settings. | Migration executed successfully; authenticated browser fixture toggles Mathematics, confirms `aria-pressed`, and observes the profile update request. | Done |
| MVP-02 | Guided practice | P2 | Check/practice learners | `client/src/hooks/useCheckState.ts`, `client/src/pages/CheckPage.tsx`, `client/src/pages/LessonPage.tsx` | Early incorrect attempts could feel terminal and completing a lesson did not expose a direct practice handoff. | Existing question flow did not expose progressive practice support consistently; final lesson navigation had no explicit practice action. | Added progressive explanation-safe hints/retry and a final Practice action that persists completion before opening the existing lesson-specific Check flow. | Desktop and mobile browser fixture observes `progress.updateProgress`, navigates to `/check/901`, and retains narration regression coverage. | Done |
| MVP-03 | Progress and continuation | P2 | Authenticated learner Dashboard and Progress page | `server/routers/progress.ts`, `client/src/pages/Dashboard.tsx`, `client/src/pages/ProgressPage.tsx` | Dashboard and Progress had generic or static learner insight surfaces. | UI lacked a persisted learner-summary contract and named concept context. | Added learner summary, named mastery details, Continue lesson, actual completion counts, and topic review recommendations from stored data only. | TypeScript passed; authenticated browser suite passed. | Done |
| MVP-04 | Learning discovery and purpose | P2 | Authenticated learner shell and lesson reading | `server/routers/curriculum.ts`, `client/src/components/AppShell.tsx`, `client/src/pages/LessonPage.tsx` | Command search exposed only fixed routes, and lesson purpose was implicit. | Search did not query course records; persisted section summaries were not shown as learning goals. | Added bounded subject/topic/lesson search and an explicit objective card derived from the current section summary. | Credential-free desktop and mobile browser test returns and renders a matching lesson result. | Done |
| MVP-05 | Remaining content-authoring boundary | P3 | Practice content and teacher authoring | `mvp_gap_assessment.md`, `client/src/hooks/useCheckState.ts` | The requirement document calls for durable authored practice content and a content-management workflow. | Current Check questions are generated per learning session and no user-approved authored item bank was supplied. | Documented this boundary rather than inventing curriculum questions, learner results, or teacher data. Existing practice UX now supports hints/retry and progress persistence. | Full validation: 10 Vitest files / 20 tests passed; browser suite: 35 passed, 1 skipped; targeted content-search suite: 10 passed. | Documented follow-up |
| MVP-06 | Onboarding and progress semantics | P2 | Keyboard and screen-reader learners | `client/src/pages/OnboardingSteps.tsx`, `client/src/pages/SettingsPage.tsx`, `client/src/pages/ProgressPage.tsx`, `e2e/tutor-voice-authenticated.spec.ts` | New learner-choice and progress surfaces needed explicit interaction semantics. | New controls could have become visual-only without role, label, and state checks. | Subject and method choices use grouped, labelled buttons with `aria-pressed`; Settings retains the same pattern; Progress retains named headings and actual lesson links. | Authenticated browser test confirms grouped subject control, keyboard-focusable button semantics, pressed state, and profile update. Full validation: 10 Vitest files / 20 tests; 41 Playwright passed, 1 skipped. | Done |

## Lesson-local voice speed control (2026-08-15)

The lesson toolbar now includes a transient voice-speed slider from **0.50×** to **2.00×** in **0.25×** steps. Its initial value mirrors the learner’s existing global speech-rate preference, but changing the lesson slider calls no profile mutation and persists nothing. The selected rate is used only when a new lesson narration utterance is created; an active utterance is intentionally not interrupted or restarted. Provider timestamp tracking, word highlighting, lesson progress, focus mode, onboarding preferences, and global Settings remain separate from the local control.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| LES-07 | Local narration rate | P2 | Lesson standard/focus modes; keyboard and screen-reader learners | `client/src/hooks/useLessonState.ts`, `client/src/pages/LessonPage.tsx`, `e2e/lesson-narration-highlight.spec.ts` | Learners could adjust global narration rate only through Settings, with no temporary lesson-level control. | Lesson narration consumed the persisted profile rate directly. | Added a controlled, labelled Radix slider scoped to lesson state; keyboard shortcuts ignore slider focus; the rate is supplied only to future lesson narration utterances. | Desktop and mobile fixture sets the slider to 1.25×, confirms the next utterance uses 1.25, and confirms no `profile.update` request is made. Full validation: 10 Vitest files / 20 tests; 43 Playwright passed, 1 skipped. | Done |

## Visual Learning Map session (2026-08-15)

The lesson-local Concept Map now presents an interactive **Visual Learning Map** without changing the data model or unrelated learning surfaces. It uses authored concept-graph data when available and falls back to lesson sections. Subject selection chooses a process, timeline, geography, literature, network, or programmatic mathematical representation. Optional illustrations are requested only for selected major concepts, reuse session and bounded server caches, and never gate the accompanying accessible text explanation. The visual layer has a visible loading state, failure retry, meaningful short alternative text, a written description, English/Arabic labels, visible focus states, and static reduced-motion-safe loading treatment.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| VLM-01 | Learning visualization and accessibility | P2 | Keyboard, screen-reader, reduced-motion, high-contrast, Dyslexia, ADHD/Focus, English/Arabic RTL, desktop and mobile learners | `client/src/components/lesson/ConceptMapSVG.tsx`, `client/src/pages/LessonPage.tsx`, `server/routers/{curriculum,tutor}.ts` | The lesson Concept Map was a passive, text-only SVG with no selected-concept detail, educational visual option, subject-aware representation, or failure-safe visual state. | Rendering derived only a fixed section-node layout and had no focused visual-generation contract or localized accessible alternative. | Replaced only the Concept Map component with a subject-aware Visual Learning Map; threaded existing graph/subject data through the lesson query; added a protected existing-helper-backed visual procedure with bounded memory cache, browser-session cache, prompt constraints, selected-major-concept prioritization, textual/programmatic fallback, and inline-tutor handoff. | `npx tsc --noEmit` passed; `pnpm test` passed (11 files / 23 tests); full `pnpm test:e2e` passed (51 passed / 1 skipped). New desktop and mobile browser cases cover keyboard selection, failure fallback, generated image alternative text, and Arabic RTL controls. | Done |
| TEST-06 | Browser-test stability | P3 | Lesson narration desktop browser fixture | `e2e/lesson-narration-highlight.spec.ts` | The existing second-session narration assertion occasionally missed the short-lived first-word marker after the playback test had already advanced. | The test asserted a transient `Alpha` state after the fixture’s tightly timed native-boundary events rather than its intended active-session guarantee. | Retained the first test’s exact word-sequence checks and changed the second-session test to verify a single active marker with the expected class. | Targeted second-session test passed; final full browser suite passed (51 passed / 1 skipped). | Done |

## Lesson provider-audio speed repair (2026-08-15)

The lesson-local voice-speed slider was already correctly passed into browser speech, but provider-backed aligned audio created an `HTMLAudioElement` without applying that same local rate. The repair applies one shared bounded rate helper to the new provider audio element before playback. It does not change the lesson state model, global profile, Settings, database, request payload, alignment data, progress, Focus mode, or stop/cleanup behavior. Provider word highlighting continues to derive from the audio element’s real media clock.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| LES-08 | Lesson-local narration speed | P1 | Standard and Focus lessons; keyboard, screen-reader, English/Arabic, desktop and mobile learners; browser and provider narration paths | `client/src/hooks/useTTS.ts`, `client/src/hooks/useTTS.test.ts`, `e2e/lesson-streamed-audio-highlight.spec.ts` | The lesson speed control changed browser utterances but did not change the audible rate when timestamp-aligned provider audio was active. | The provider branch created `new Audio(url)` and started playback without assigning the hook’s existing `rate` to `audio.playbackRate`. | Added a shared 0.5×–2.0× normalization helper and set provider `audio.playbackRate` before `play()`; retained the existing browser-utterance assignment and all state/request/cleanup paths. | TypeScript passed. Unit suite: 11 files / 24 tests, including rate normalization. Provider fixture confirms 1.5× reaches the audio element, preserves timestamped word progression, and emits no `profile.update`; full browser suite: 53 passed / 1 skipped across desktop and mobile. | Done |

## Visual Learning Map selection-contrast correction (2026-08-16)

The selected concept tile now has a semantic selected marker, stronger outline, elevation, and full foreground contrast rather than a background-color-only treatment. Directly connected concepts receive a distinct relationship icon, explanatory label, and secondary emphasis; all other concepts retain readable card styling and are not heavily dimmed. The existing map has no rendered connection-line geometry, so its authored direct-relationship data is represented through these existing concept tiles rather than by adding new visualization behavior. The supplied recording showed unrelated dashboard cards, which were intentionally left unchanged because the written scope explicitly restricted this work to the Visual Learning Map.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| VLM-02 | Selected concept visibility | P2 | Keyboard and screen-reader learners; high-contrast and reduced-motion profiles; English/Arabic; desktop and mobile | `client/src/components/lesson/ConceptMapSVG.tsx`, `e2e/lesson-narration-highlight.spec.ts` | A selected concept could visually blend into nearby cards because selection depended mainly on a primary background color and offered no distinct direct-relationship hierarchy. | The local concept-tile renderer had only selected/unselected color treatments, despite already holding direct relationship IDs. | Kept data flow and interactions intact; added selected/related/background semantic state attributes, a check marker and outline/elevation for selection, a connection marker/label and secondary emphasis for direct relationships, and a stronger token-based keyboard focus ring. | TypeScript passed. Unit suite: 11 files / 24 tests passed. Browser suite: 55 passed / 1 skipped. Desktop and mobile checks cover keyboard focus, selection transition, selected/related/background state hierarchy, Arabic RTL controls, semantic high-contrast foreground/background treatment, and reduced-motion transition suppression. | Done |

## Minimalist UX and durable learning preferences (2026-08-16)

HIKMA already had meaningful accessibility, curriculum, modality, audio, subject-priority, and daily-study settings. This update closes the verified personalization gaps rather than adding a separate learner-data system: learners can now save adjustable goals, explanation depth, practice style, and session pace alongside those existing preferences. The onboarding flow remains multi-step and adds a short goals step; final completion now explicitly persists its completion state. The learner home keeps an in-progress lesson first, uses priority subjects only when a new learner needs a starting lesson, explains that recommendation in neutral language, and reduces fixed quick access from three cards to two. Tutor requests now include the adjustable learning preferences, while the tutor prompt instructs it to favour actual questions and demonstrated understanding over declared choices.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| PER-01 | Durable, functional personalization | P1 | New and returning learners; English/Arabic; standard, audio-first, Focus/ADHD, Dyslexia, low-vision, desktop and mobile | `drizzle/schema.ts`, `drizzle/0007_quiet_runaways.sql`, `server/routers/{profile,progress}.ts`, `client/src/{contexts/ProfileContext.tsx,pages/Onboarding.tsx,pages/OnboardingSteps.tsx,pages/SettingsPage.tsx,pages/Dashboard.tsx,hooks/useTutorState.ts}`, `server/tutorStream.ts` | Existing onboarding and Settings saved curriculum, subject priorities, modality, and accessibility settings, but did not persist explicit goals, explanation depth, practice format, or session pace; home recommendations were performance-only and tutor context omitted learning preferences. | The learner profile and tRPC contract lacked the four specific fields, and the home/tutor paths consumed only the older accessibility/curriculum fields. | Added four additive profile fields and a reviewed TiDB-compatible migration; added an eight-step, keyboard-operable onboarding flow with purpose-bound preference controls; persists onboarding completion; exposes all additions in the existing real-time Settings editor; selects a priority-subject lesson only when no in-progress lesson exists; gives neutral recommendation rationale; forwards preferences through the existing tutor stream; clarifies shared navigation as Home, Learn, AI Tutor, Progress, and Practice. | `npx tsc --noEmit` passed. Unit suite: 11 files / 24 tests passed. Focused authenticated desktop/mobile browser suite: 18 passed. Full browser suite: 61 passed / 1 skipped. New coverage verifies Settings goal persistence, a visible priority-subject recommendation while preserving the primary continuation action, and the Arabic learning-goals step. Existing desktop/mobile checks retain high-contrast, reduced-motion, tutor voice, keyboard, and navigation coverage. | Done |

## Feature-aware onboarding and voice-command repair (2026-08-16)

The live curriculum inventory currently confirms **three usable subjects** with active lessons: Mathematics, English Language, and Science (Double), all under IGCSE Edexcel. Onboarding and Settings now consume a single server-backed usable-subject procedure, so Arabic remains an interface locale rather than an invented subject option; unserved curricula and the unsaved year-group question are no longer presented. Subject priority values now use the corresponding live subject code and influence the existing new-learner recommendation path, with legacy values retained for existing profiles.

Global voice commands now follow a single explicit sequence: permission request, listening, transcript, understanding, visible execution feedback, supported action, or a text fallback. The command parser maps only to real routes and live subject IDs, supports common English and Arabic variants, removes the orphaned answer submission command, normalizes Arabic diacritics for recognition, and clearly reports unsupported requests such as an unavailable Arabic subject. Browser microphone access is requested only to obtain permission; the stream is stopped immediately, and no raw audio is stored by this flow.

| ID | Category | Severity | Profiles/locales affected | Location (file:line) | Symptom | Root cause | Fix | Verified how | Status |
|---|---|---|---|---|---|---|---|---|---|
| CURR-01 | Content truthfulness and onboarding | P1 | New and returning learners; English/Arabic RTL; keyboard; desktop/mobile | `server/routers/curriculum.ts`, `client/src/pages/{OnboardingSteps,SettingsPage,Onboarding}.tsx`, `server/routers/progress.ts` | Onboarding offered Arabic, Exam Skills, and multiple unserved curricula as subject/content choices; year group was collected but never persisted or used. | The client used static option arrays that had drifted from active subjects with active lessons, while priority matching expected legacy display values. | Added one live `availableSubjects` contract filtered to active curricula, subjects, topics, and lessons; rendered it in onboarding and Settings; removed unserved/static subject and curriculum choices and the unused year-group prompt; applied live subject codes to preference-aware recommendation matching with legacy compatibility. | Live database inventory confirmed three usable IGCSE Edexcel subjects and 11 active lessons. Browser fixtures verify Mathematics, English Language, and Science (Double) appear in Arabic onboarding while Arabic does not; Settings persistence uses the live Mathematics code. TypeScript passed; unit suite: 50 tests passed; final browser suite: 69 passed, 1 skipped. | Done |
| VOI-01 | Voice command execution and recovery | P1 | Blind/low-vision, keyboard, reduced-motion, English/Arabic, desktop/mobile | `client/src/hooks/useVoiceCommands.ts`, `client/src/components/VoiceCommandOverlay.tsx`, `client/src/pages/LessonPage.tsx`, `e2e/tutor-voice-authenticated.spec.ts` | Recognized subject commands opened only a generic subject page, unsupported feature requests were sent to the tutor, action states were limited to on/off, and one command emitted an event with no listener. | Command patterns and subject paths were hard-coded; the recognition hook had no permission/processing/executing/error state model or source-of-truth subject input; `answer_question` had no application listener. | Added typed state/status feedback, transcript display, bilingual diacritic-normalized parsing, live subject-route matching, clear delayed navigation confirmation, unsupported-command feedback, permission/unavailable recovery, and a visible Type instead path; removed the orphaned command while retaining existing lesson read/next/previous events and tutor questions. | 26 unit cases cover supported English/Arabic route variants, live Mathematics/English/Science commands, unavailable Arabic, and tutor-question fallback. Desktop/mobile browser cases verify live subject execution feedback, unsupported-command recovery, denied microphone recovery, Arabic onboarding parity, keyboard-operable controls, and text fallback. Real microphone hardware/noise recognition was not exercised in automated tests. TypeScript passed; unit suite: 50 tests passed; final browser suite: 69 passed, 1 skipped. | Done |
